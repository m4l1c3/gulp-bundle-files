var gulp = require('gulp'),
    gulpBundleFiles = require('gulp-bundle-files'),
    bundles = require('options.json');

gulp.task('bundle', function() {
    return gulp.src()
});

gulp.task('default', ['bundle']);